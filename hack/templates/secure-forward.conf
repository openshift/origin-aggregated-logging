<store>
  @type forward

  <security>
    self_hostname forwarding-${HOSTNAME}
    shared_key aggregated_logging_ci_testing
  </security>

  <server>
    host ${FLUENTD_FORWARD}
    port 24284
  </server>
</store>
