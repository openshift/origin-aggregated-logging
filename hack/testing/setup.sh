#!/bin/bash

# This script sets up a local OpenShift cluster and
# installs logging components on it in order to run
# the logging test suites.

source "$(dirname "${BASH_SOURCE[0]}" )/../lib/init.sh"

os::log::fatal "Cluster setup is not yet implemented."