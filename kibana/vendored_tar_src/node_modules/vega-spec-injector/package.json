{
  "name": "vega-spec-injector",
  "version": "0.0.2",
  "description": "Vega helper library to simplify modification of the Vega and VegaLite JSON before parsing.",
  "main": "cjs/index.js",
  "browser": "dist/vegaSpecInjector.js",
  "module": "es/index.js",
  "jsxnext:main": "es/index.js",
  "files": [
    "dist",
    "cjs",
    "es",
    "src"
  ],
  "engines": {
    "node": ">=4.0.0"
  },
  "scripts": {
    "prepublish": "npm run build",
    "precommit": "npm run lint:test",
    "prepush": "npm run lint:test",
    "release": "release-it",
    "coveralls": "cat ./coverage/lcov.info | ./node_modules/.bin/coveralls",
    "lint": "npm run lint:eslint",
    "lint:eslint": "eslint src/*.js",
    "test": "clear && jest",
    "test:coverage": "jest --coverage ",
    "test:watch": "clear && jest --watch",
    "lint:test": "npm run lint && npm run test:coverage",
    "build": "clear && npm run lint:test && npm run build:cjs && npm run build:es && npm run build:umd",
    "build:watch": "clear && rimraf cjs && cross-env BABEL_ENV=cjs babel -w src --out-dir cjs",
    "build:es": "rimraf es && cross-env BABEL_ENV=es babel src --out-dir es",
    "build:cjs": "rimraf cjs && cross-env BABEL_ENV=cjs babel src --out-dir cjs",
    "build:umd": "rimraf dist && cross-env BABEL_ENV=es rollup -c & cross-env BABEL_ENV=es NODE_ENV=production rollup -c"
  },
  "keywords": [],
  "author": "Yuri Astrakhan (YuriAstrakhan@gmail.com)",
  "license": "MIT",
  "repository": "nyurik/vega-spec-injector",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-preset-es2015": "^6.24.1",
    "coveralls": "^2.13.1",
    "cross-env": "^5.0.5",
    "eslint": "^4.6.0",
    "eslint-config-devine": "^1.7.1",
    "eslint-plugin-babel": "^4.1.2",
    "jest": "^20.0.4",
    "rimraf": "^2.6.1",
    "release-it": "^2.8.5",
    "husky": "^0.14.3",
    "rollup": "^0.39.2",
    "rollup-plugin-babel": "^3.0.2",
    "rollup-plugin-node-resolve": "^3.0.0",
    "rollup-plugin-bundle-size": "^1.0.1",
    "rollup-plugin-commonjs": "^8.2.0",
    "rollup-plugin-uglify": "^2.0.1"
  }
}
